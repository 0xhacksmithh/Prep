# 11. What are query params vs path params?

✅ Answer
| Path Params | Query Params |
| ------------------- | ------------------------ |
| Identify a resource | Filter / modify response |
| Mandatory | Optional |
| Part of URL path | After `?` |
| RESTful | Not resource identity |

### 📊 Example

```
GET /users/123        ← Path param (resource)
GET /users?role=admin ← Query param (filter)
```

### 📊 Diagram

```
/users/{id}
        ↑
   Path Param
```

### 🔐 Security Tip

- Always validate query params

- Prevent injection & excessive filtering

# 12. What is pagination? Offset vs Cursor pagination?

✅ Answer

Pagination limits the amount of data returned per request.

### 📌 Offset Pagination

```
GET /users?page=2&limit=10
```

### ❌ Problems:

- Slow for large datasets

- Data inconsistency if records change

### 📌 Cursor Pagination

```
GET /users?cursor=abc123&limit=10
```

### ✅ Benefits:

- Fast

- Consistent

- Scalable

### 📊 Diagram

```
Offset:
[1][2][3][4][5] → page=2

Cursor:
[...][lastId=42] → next page
```

### 🔐 Security Note

Use **opaque cursors** to prevent data enumeration.

# 13. What is rate limiting?

✅ Answer

Rate limiting restricts the **number of requests per client** in a given time window.

### 📌 Example

```
100 requests / minute / user
```

### 📊 Diagram

```
Client → API Gateway → API
           |
      Rate Limit Check
```

### 🔐 Why Important?

- Prevent brute-force attacks

- Protect server resources

# 14. What is throttling?

✅ Answer

Throttling **slows down** requests instead of blocking them.

### 📊 Difference

| Rate Limiting | Throttling       |
| ------------- | ---------------- |
| Hard limit    | Gradual slowdown |
| Returns 429   | Delays response  |
| Security      | QoS control      |

### 📊 Diagram

```
High Traffic
   ↓
Throttle
   ↓
Delayed Responses
```

# 15. What is an API Gateway?

✅ Answer

An API Gateway is a **single entry point** for all API requests.

### 📌 Responsibilities

- Authentication

- Rate limiting

- Logging

- Routing

### 📊 Diagram

```
Client
   |
API Gateway
 |   |   |
Auth API Orders API Users API
```

### 🔐 Security Benefit

Centralized security enforcement

# 16. Authentication vs Authorization

✅ Answer
| Authentication | Authorization |
| -------------- | --------------- |
| Who you are | What you can do |
| Login | Access control |
| Happens first | Happens after |

### 📊 Diagram

```
User → Auth → Token → Access Check → Resource
```

### 🔐 Interview Tip

AuthN before AuthZ — always

# 17. What is Basic Authentication?

✅ Answer

Basic Auth sends `username:password` encoded in Base64.

### 📊 Header Example

```
Authorization: Basic dXNlcjpwYXNz
```

### 📊 Diagram

```
Client → Base64 Creds → Server
```

### 🔐 Security Issues

- Easily decoded

- Must use HTTPS

- Not scalable

# 18. What are API keys? Are they secure?

✅ Answer

API keys identify **calling applications**, not users.

### 📊 Example

```
x-api-key: abc123
```

### 📊 Diagram

```
Client → API Key → API
```

### 🔐 Security Reality

❌ Not secure alone
✅ Combine with:

- IP restriction

- Rate limits

- Rotation

# 19. What is token-based authentication?

✅ Answer

Client receives a token after login and sends it with each request.

### 📊 Diagram

```
Login → Token Issued
Client → Token → API
```

### 🔐 Advantages

- Stateless

- Scalable

- Mobile friendly

# 20. What is JWT? Structure of JWT?

✅ Answer

JWT = JSON Web Token (signed token).

### 📊 Structure

```
HEADER.PAYLOAD.SIGNATURE
```

### 📊 Diagram

```
Header → Algorithm
Payload → Claims
Signature → Integrity
```

### 🔐 Important

- Do NOT store sensitive data

- Always verify signature
